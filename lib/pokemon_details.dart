import 'package:flutter/material.dart';

class PokemonDetail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: SafeArea(child: Text('pokemon details')),
    );
  }
}
